import { styled } from '@mui/material';

export const Saly = styled('img')({
    height: '42vw',
    minHeight: '300px',
    position: 'absolute',
    top: '5%',
    left: '11%'
})

export const GoogleIcon = styled('img')({
    height: 20,
})